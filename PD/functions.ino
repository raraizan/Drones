
float b = 1, k = 1, l = 1, g = 9.81, m = 1;
float Ixx = 1, Iyy = 1, Izz = 1;

int[] omega = {1500, 1500, 1500, 1500};
float[] constants = {1, //kzp
                     1, //kzi
                     1, //kzd
                     1, //krp
                     1, //kri
                     1, //krd
                     1, //kpp
                     1, //kpi
                     1, //kpd
                     1, //kyp
                     1, //kyi
                     1 //kyd
                    };

void P() {

  float T = m * g / (cos(roll) * cos(pitch))
    float tr = constants[] * ()
    float tp
    float ty


  for (int i = 0; i < omega.length; i++) {
    omega[i] = constrain(omega[i], 1500, 2000);

  }

}

